spring:
  datasource:
    url: jdbc:mysql://localhost:3306/order-db
    username: root		
    password: mysql
  jpa:
    hibernate:
        ddl-auto: none
    show-sql: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs

server:
  port: 8081

inventoryservice:
  url: http://localhost:8082 
